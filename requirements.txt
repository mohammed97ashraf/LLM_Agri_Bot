# =============================================================================
# LLM Agri Bot - Python Dependencies
# =============================================================================
# Preferred: use `uv sync` instead (reads from pyproject.toml)
# This file is kept for compatibility with pip/Render.
#
# Core
flask>=3.1.3
python-dotenv>=1.0.1

# LLM (Groq SDK)
groq>=0.30.0

# Redis (conversation memory)
redis>=5.2.0

# HTTP / API
requests>=2.32.0

# Production server
gunicorn>=22.0.0
